{
  "name": "agent",
  "version": "1.0.0",
  "description": "2D Typescript game engine",
  "main": "src/agent.ts",
  "scripts": {
    "build": "tsc --diagnostics",
    "build-compress": "npm run build && uglifyjs ./build/**/*.js --compress -o ./out/agent2d.min.js",
    "build-test": "npm run build && npm run test",
    "test": "mocha ./test/**/*.js"
  },
  "author": {
    "name": "Joe Berria",
    "email": "joeberria@gmail.com"
  },
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.0.0",
    "tslint": "^5.9.1",
    "uglify-es": "^3.3.9"
  }
}
